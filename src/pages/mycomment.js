import { useEffect } from "react"
import { UseContextFunction } from "../Hooks/useWorkoutContext"

const MyComments = ({newComs}) => {
    // const {comments,dispatch} =UseContextFunction()

    // console.log(newComs)

    return ( 
<div>

   
</div>

     );
}
 
export default MyComments;